spring:
  #部署时使用的配置文件
  profiles:
    active: pro
  messages:
    basename: ValidationMessages
    encoding: utf-8
  #返回的json设置
  jackson:
    default-property-inclusion: non_null
server:
  port: 7500